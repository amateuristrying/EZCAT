import AppShell from '../src/screens/app/AppShell';

/**
 * Route: /home
 * The main app (post-onboarding) with its four bottom tabs.
 */
export default function HomeRoute() {
  return <AppShell />;
}
