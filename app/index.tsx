import WelcomeScreen from '../src/screens/WelcomeScreen';

/**
 * index.tsx — Route "/"
 * Renders Screen 01: EZCAT Welcome.
 * SafeAreaProvider and MobileFrame are provided by _layout.tsx.
 */
export default function Index() {
  return <WelcomeScreen />;
}
