import LevelScreen from '../../src/screens/LevelScreen';

/**
 * Route: /onboarding/level
 * Screen 05 — How would you rate your current level in each section?
 */
export default function LevelRoute() {
  return <LevelScreen />;
}
