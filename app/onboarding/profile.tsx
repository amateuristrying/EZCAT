import ProfileScreen from '../../src/screens/ProfileScreen';

/**
 * Route: /onboarding/profile
 * Screen 02 — Tell us about yourself.
 */
export default function ProfileRoute() {
  return <ProfileScreen />;
}
