import ReadyScreen from '../../src/screens/ReadyScreen';

/**
 * Route: /onboarding/ready
 * Screen 06 — You're Ready! (final onboarding step)
 */
export default function ReadyRoute() {
  return <ReadyScreen />;
}
